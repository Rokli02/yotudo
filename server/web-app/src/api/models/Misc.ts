export interface Genre {
    id: number;
    name: string;
}

export interface Status {
    id: number;
    name: string;
    description: string;
}

export interface GenreDTO {
    Id: number;
    Name: string;
}

export interface StatusDTO {
    Id: number;
    Name: string;
    Description: string;
}